{
  "name": "@nextgen/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:gate": "playwright test --grep-invert @quarantine",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "report-summary": "node scripts/report-summary.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
